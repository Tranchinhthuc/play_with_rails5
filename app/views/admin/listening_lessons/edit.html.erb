<%= simple_form_for(:examination,
  url: Rails.application.routes.url_helpers.send("admin_#{controller_name.singularize}_path", @listening_lesson),
  method: :patch) do |f| %>
  <%= render "form", f: f %>
<% end %>

