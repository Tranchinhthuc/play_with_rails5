<%= simple_form_for([:admin, instance_variable_get(:"@#{controller_name.singularize}")],
  url: Rails.application.routes.url_helpers.send("admin_#{controller_name.singularize}_path", instance_variable_get(:"@#{controller_name.singularize}")),
  method: :patch) do |f| %>
  <%= render "form", f: f %>
<% end %>
