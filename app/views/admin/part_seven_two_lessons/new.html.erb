<%= simple_form_for([:admin, instance_variable_get(:"@#{controller_name.singularize}")],
  url: Rails.application.routes.url_helpers.send("admin_#{controller_name}_path"),
  method: :post) do |f| %>
  <%= render "form", f: f %>
<% end %>
